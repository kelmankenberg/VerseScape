import { PagePlaceholder } from '../components/PagePlaceholder.js';

export function LibraryPage(): React.JSX.Element {
  return (
    <PagePlaceholder
      title="Library"
      description="Installed Bibles and commentaries, imports, and catalogue downloads."
      milestone="M6"
    />
  );
}
