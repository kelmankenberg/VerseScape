import { PagePlaceholder } from '../components/PagePlaceholder.js';

export function PlansPage(): React.JSX.Element {
  return (
    <PagePlaceholder
      title="Reading Plans"
      description="Start a plan, track daily progress, and keep a streak."
      milestone="M7"
    />
  );
}
