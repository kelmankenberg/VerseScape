import { PagePlaceholder } from '../components/PagePlaceholder.js';

export function NotesPage(): React.JSX.Element {
  return (
    <PagePlaceholder
      title="Notes"
      description="Notebooks, rich-text notes anchored to references, and highlights."
      milestone="M5"
    />
  );
}
